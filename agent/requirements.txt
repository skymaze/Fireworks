# 生产锁定：精确版本。Agent 在镜像构建时安装（容器化部署，节点零网络依赖）。
# uvicorn 不带 [standard]：uvloop/httptools 是平台+Python 版本强相关包，
# agent 流量小用不到性能加速；WS 支持由显式 websockets 提供（镜像自带）。
fastapi==0.141.1
uvicorn==0.52.1
websockets==16.1.1
psutil==7.2.2
