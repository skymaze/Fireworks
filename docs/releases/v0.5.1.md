# Fireworks v0.5.1

前端补丁：修复任务详情「健康」列 i18n key 显示原文、停止任务确认框不再显示「删除模型」选项。

## 变更

- 健康列 5 个 i18n key 移入 `tasks` 对象（此前误插 `clusters`，导致显示 `tasks.col_health` 等原文）
- 停止任务不再提供「同时删除节点模型」选项（仅删除任务可勾选）

## 升级

- 控制面与前端升级到 `0.5.1`；纯前端修复，无后端/数据变更，无需迁移；节点 Agent 无需重部署
- 升级前建议备份 `fireworks-db` 卷

完整变更见 [CHANGELOG](https://github.com/skymaze/Fireworks/blob/v0.5.1/CHANGELOG.md)。
